#List
list=[X,Y,Z]

#Defining the variables
X=X.extend(int())
Y=Y.extend(float())
Z=''

#List for sorting
list.sort[X,Y,Z]

#Return
my_dict={evens:even_numbers,odds:odd_numbers,chars:''}

for even_numbers in X 
         if even_numbers%2==0:
             return(isinstance(even_numbers,int)
                    else:
                    return()
for odd_numbers in X and Y
                    if odd_numbers%2==1:
                    return(isinstance(odd_numbers,int or float)
                           else:
                           return()
for chars in Z                           
                           if chars==str:
                           return(isinstance(chars,str)
                                  else:
                                  return()
    
d={evens:[sorted(even_numbers)],odds:sorted[(odd_numbers)],chars:[sorted('')]}

 print(d)   
